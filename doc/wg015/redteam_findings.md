# Red-team findings по PLAN.md draft v1

Панель: 5 линз; выполнены 2 (timing, flash), 3 упали на лимите сессии (boot, arch, complete)
— перезапуск: см. STATE.md. Источник: workflow wf_8f218f26-7c7 journal.

## Линза: ?

**Вердикт:** The plan's factual base and phase ordering are strong, but its centerpiece — the G1 gate — is designed in a way that systematically favors a false PASS for flash-first: the P1.4/P1.5 benches model neither I-cache eviction by the arbitrary user main loop and the C protocol layer (H1 tested on an unrealistically warm cache), nor the real RX engine's data-dependent multi-path fetch pattern (H2 is already refuted at the documented-prefetcher level — every per-bit backward branch leaves the 32-byte window — so flash-first silently rests on the undocumented I-cache alone), nor D-code flash reads that the TX loop performs on flash-resident descriptors (H3's 'all data in RAM' is un-enacted and collides with the anti-scope). The acceptance algebra is also wrong: slot errors integrate into phase with no in-packet resync, so 'per-slot ≤±2' either admits fatal accumulation or is vacuous next to the −4..+6 total-drift bound, which itself is a repurposed V003 start-phase artifact — and the proposed period histogram cannot even produce the cumulative quantity being gated. Finally, both fallbacks are mis-costed ('не трогает код' is contradicted by the repo's own lui/jr RAM-variant precedent across the 1 GB flash↔TCM gap), and LAT=1 is adopted without an integrity gate against unanimous vendor/community LAT=3 practice. Executed as written, G1 would likely pass and P3/P4 would fail with hard-to-attribute timing losses — the exact class of expensive rework the calibration phase exists to prevent. Fix the gate's benches, criterion, and fallback costing before P1; the rest of the plan is sound.

### ?-1 [BLOCKER]
**Претензия:** G1 gate (P1.4a 'холодный/горячий прогон (H1: повтор == повтор?)' and P1.5 slot emulation) measures an unrealistically warm I-cache: no bench models eviction of ISR lines by code that shares the same undocumented 2KB I-cache — the arbitrary user main loop, the C protocol layer, libc. H1's promise 'повторные пакеты детерминированы' is tested only in a quiet loop; in the real system the first traversal of each RX sub-path (bit-stuff path, EOB path, one-bit path) inside a given packet hits a cold/evicted line MID-packet, after the preamble retiming has already locked the grid — the SYNC absorber (PLAN §3 'вход ISR съедается SYNC-паттерном') cannot save mid-packet cold misses. So the gate is structurally biased toward PASS, and failure surfaces at P3 as unexplained CRC losses — the exact expensive rework G1 exists to prevent.

**Доказательство:** PLAN Р6: «H1: горячий ISR (~1.2К) целиком садится в I-cache 2К → повторные пакеты детерминированы»; PLAN P1.4(а): «холодный/горячий прогон (H1: повтор == повтор?)» — no eviction adversary anywhere in P1. Hot set is larger than the ISR: C handlers run inside the same ISR invocation (rv003usb.S:492 `c.beqz a5, usb_pid_handle_in` → rv003usb.c:162-238 in flash .text, descriptor scan c:415-433), plus user main loop between packets and keepalive every 1 ms (S:673). I-cache organization/line size/eviction: undocumented — research_flash.md GAPS 3: «Core 2 KB I-cache: organization, line size, enable/bypass/flush controls … absent from РП». First-touch of handle_bit_stuff occurs only at the first stuffed bit of a packet (rv003usb.S:396-424), i.e. mid-grid.

**Фикс:** Redesign P1.4a/P1.5: between emulated packets run an eviction adversary (execute >2-4 KB of unrelated flash code, e.g. jr through a jump table); start emulated packets with one sub-path deliberately cold (first touched mid-packet). G1 pass requires timing identical with vs without the adversary. State in Р6 that H1's 'ISR' must include the C handlers on the turnaround path.

### ?-2 [BLOCKER]
**Претензия:** H2 ('straight-line код в пределах префетч-буферов = 0ws, miss только на дальних бранчах') is factually wrong for the actual RX engine, and the P1.5 synthetic slot ('load-DATA + работа + store-TGL') is far more linear than the real code, so it cannot falsify H2. The RX bit loop is NOT straight-line: each bit takes a data-dependent path among bit_process → handle_zero_bit / handle_one_bit / handle_bit_stuff / is_end_of_byte, spanning roughly S:298–S:424 (~300 bytes) with a backward branch every bit — every one of those targets is 'дальний' relative to the documented 2×128-bit (32-byte) prefetch window, so at the documented-hardware level EVERY slot misses, and the miss pattern varies with packet content (data-dependent jitter). Flash-first therefore rests entirely on the undocumented I-cache, which the plan never states. A tight synthetic loop fits in the two buffers (or trivially in cache) and will show zero jitter no matter what the real code would do → G1 false-pass.

**Доказательство:** rv003usb.S:304-424: `c.beqz a0, handle_one_bit` (S:323), `c.beqz s0, handle_bit_stuff` (S:382), `HANDLE_NEXT_BYTE(is_end_of_byte,…)` (S:313-317, 351, 384, 400), `c.bnez s1, bit_process` (S:362, 393, 424) — multi-target, content-selected paths. research_flash.md §3: prefetcher = 2×128-bit buffers, «miss both → goto 1», «РП gives no cycle formula for miss cost»; research_core_irq.md §6: «every branch out of the 32-byte buffered window eats a nondeterministic flash-line refill». PLAN P1.5: «синтетический 32-тактный цикл (load-DATA + работа + store-TGL)» — single body, one branch.

**Фикс:** Make the P1.5 emulation structurally isomorphic to the real slot: ≥3 alternative slot bodies placed at realistic distances (100–300 B apart), selected per-iteration by PRBS data, backward branch each slot — or port the RX skeleton (zones z1/z2 only) early and drive it with a pattern generator before declaring G1. Rewrite H2 honestly: 'flash-first is possible only if the undocumented I-cache makes the multi-path loop deterministic'.

### ?-3 [BLOCKER]
**Претензия:** G1 criterion algebra is wrong. (a) Slot durations integrate: the sampling grid IS the instruction stream and master has no in-packet resync (the plan anti-scopes the branchless resync), so per-slot error accumulates as phase — 'per-slot джиттер ≤±2' admits a random walk (±2·√90 ≈ ±19 cycles 1σ-scale excursions) or a systematic bias of up to 62 500 ppm, 6.8× outside master's own measured eye tolerance (9217 ppm). (b) Enforcing the second criterion (90-bit drift within −4..+6) implies mean per-slot error ≤6/90 ≈ 0.07 cycles — making the ±2 criterion vacuous or misleading. (c) The proposed measurement, «гистограмма периодов», mathematically cannot distinguish correlated from independent jitter and cannot reconstruct worst-case cumulative excursion, i.e. the bench does not even produce the quantity the second criterion gates on. (d) Most fundamentally: the codebase's only timing tool is STATIC padding, which can correct a deterministic per-path constant but can never correct genuine nondeterminism — so measured true jitter of ±2 should mean FAIL-to-flash, yet the gate passes it.

**Доказательство:** PLAN G1: «per-slot джиттер ≤±2 тактов и суммарный дрейф на 90-битном пакете в окне −4..+6 → флеш»; P1.5: «гистограмма периодов — прямой ответ». No in-packet resync in master: rv003usb.S:327-331 «Can we resync time here? … I was thinking we could put the resync code here» (unimplemented TODO); PLAN §6 anti-scope excludes the branchless resync branch. Padding-only method: rv003usb.S:20 `nx6p3delay`, S:22-25 `VOOXDELAY`; stack_portability.md §2 «Loops are hand-unrolled instruction slots padded to 32 cycles». Eye tolerance scale: branch_notes.md — master eye 9217 ppm (ones); «worst-case all-ones payload dies at ~+5000 ppm» without resync; runfromram warning: «changing fetch timing silently breaks the cycle ledgers».

**Фикс:** Replace the criterion: (1) each RX/TX path must be repeat-DETERMINISTIC (zero variance) across repeats and cache-state preparations — any true nondeterminism ⇒ flash FAILS, since only static padding exists; (2) per-path means trimmed to exactly 32 by z7; (3) residual drift gated by max cumulative excursion over sliding 90-slot windows across ≥10^4 PRBS packets (log cumulative sums, not just a period histogram).

### ?-4 [MAJOR]
**Претензия:** The −4..+6 number is a start-of-grid PHASE-skew tolerance measured once, at done_preamble, on CH32V003 with that build's specific sample placement — the plan repurposes it as the accumulated-drift budget over a 90-bit packet in G1. These are different physical quantities (start-phase window vs sustained slip tolerance; master's eye sims tolerate ~9200 ppm ≈ tens of cycles of end-of-packet slip, inconsistent with a 10-cycle window), and the window's asymmetry is an artifact of V003 slot placement. On WG015 the placement changes (different padding z7, +2-cycle input synchronizer which the plan itself calls a constant phase shift, different lw latency), so the acceptance envelope is not transferable.

**Доказательство:** rv003usb.S:186-188: «8 extra cycles here cause errors. -5 cycles is too much. -4 to +6 cycles is OK» — a comment at done_preamble about extra cycles AT THAT POINT, on V003. PLAN §3 cites it as «допуск сдвига фазы выборки −4..+6 тактов после преамбулы (S:186-188)» then G1 reuses it as «суммарный дрейф на 90-битном пакете в окне −4..+6». PLAN §3 itself: «+2 такта входного синхронизатора — константа, сдвигает фазу» — never subtracted from the envelope. branch_notes.md: master eye 9217/10389/10272 ppm.

**Фикс:** Re-derive the envelope on the target: measure the start-phase window on the ported slot by sweeping artificial skew (the ±1-cycle margin sweep already planned for P6 — move it to P3/G1), budget = (window after WG015 re-padding) − (sync constant) − (measured worst cumulative excursion). Do not hard-code S:186-188 numbers into the gate.

### ?-5 [MAJOR]
**Претензия:** H3 ('все данные ISR в RAM' — 'принимается сразу как правило (дёшево)') is neither cheap nor sufficient: USB descriptors are const .rodata in flash, and the TX inner loop reads descriptor bytes from flash INSIDE the cycle-counted bit loop — a D-code flash read every 8 TX bits, with РП-documented D-code-over-I-code priority stalling instruction fetch mid-slot. Enforcing H3 literally means copying all descriptors to RAM with pointer fixup — a C-layer change the plan's own Р3 ('rv003usb.c трогаем минимально') and anti-scope ('изменения C-протокольного слоя сверх швов Р3') forbid, and for which no plan step exists. Leaving them in flash means P1/G1 never measured the dominant TX perturbation class (D-code + in-flight I-prefetch collision), so GET_DESCRIPTOR transfers can jitter even after G1 passes → discovered at P4.

**Доказательство:** rv003usb.c:424: `e->opaque = (uint8_t*)dl->addr;` (descriptor_list = const, flash); rv003usb.c:236 `usb_send_data( sendnow, tosend, 0, sendtok )`; rv003usb.S:887-890 `load_next_byte: XW_C_LBU(s0, a0, 0)` — byte load inside the counted TX loop. research_flash.md §3: «Чтение D-code шины имеет приоритет» (РП 7.1, manual.txt:2014-2015) — «jitter source if constants live in flash». PLAN Р6 H3 wording + PLAN §6 anti-scope + Р3.

**Фикс:** Decide now, in Р6/Р8: either (a) add an explicit init-time copy of descriptor payloads to RAM (small, bounded; makes H3 actually true — acknowledge the C-seam cost), or (b) add a flash-resident lbu into the P1.5 TX-slot emulation and a G1 sub-criterion for D-code/I-fetch collision determinism. Silence guarantees rework at P4.

### ?-6 [MAJOR]
**Претензия:** Р6's cost claim for the TCM fallback — «цена нулевая … переключение при провале G1 не трогает код», R1: «код не меняется» — is factually wrong. Flash (0x8000_0000) and TCM-A (0x4000_0000) are 1 GB apart: moving the ISR section alone breaks every asm↔flash cross-reference (conditional `c.beqz a5, usb_pid_handle_in` to the flash-resident C handler has ±256 B reach; `jal`/`j` reach ±1 MB). The repo's own precedent proves it: the CH32V00x RAM variant had to rewrite those very branch sites into lui/addi + c.jr sequences with extra cycles in the turnaround-critical dispatch path. The Р7 hybrid (head stub in TCM, body in flash) additionally inserts an unmeasured TCM→flash fetch transition mid-preamble that no P1 bench characterizes.

**Доказательство:** PLAN Р6, R1, Р7. rv003usb.S:461/492 `c.beqz a5, usb_pid_handle_ack/…in` (C handlers in flash: rv003usb.c:162); rv003usb.S:465-470, 497-515, 724-727 — the `#if CH32V00x` lui+jr replacements; stack_portability.md §2: «flash↔RAM calls need lui/addi + c.jr because offsets exceed ±1 MB j range (S:465-470, 498-514, 725-727; notes:17)». Memory map: research_flash.md §6 (RAM0 @0x4000_0000, flash @0x8000_0000).

**Фикс:** Re-cost R1/R2: fallback = section move PLUS V00x-style far-branch trampolines with re-derived dispatch cycle counts. Prepare the trampoline `#if`-zone in P2/P3 (it is already zone-shaped in the V00x precedent) so that by G1-fail time the switch really is near-link-time. Delete the «цена нулевая»/«код не меняется» wording.

### ?-7 [MAJOR]
**Претензия:** LAT=1 is adopted as settled fact (F2 «LAT=1 (не 3!)», Р4) purely from РП табл. 7.1, while the plan under-weights the silicon-margin signal: vendor AND community code universally run LAT=3+CEN=1 at 50 MHz, an official erratum proves a real flash-fetch pipeline hazard exists on this die, and the fact base separately proves РП register tables contain errors. No P1 bench checks read INTEGRITY at LAT=1 (all benches measure cycles, not correctness), and the §5 risk table has no row for 'LAT=1 marginal → escalate to LAT=2/3', even though escalation changes every P1-measured number and could flip G1 after the fact.

**Доказательство:** PLAN F2/Р4; §5 has no LAT risk row. research_errata.md TAKEAWAY 4: «Vendor + community init both use FLASH LAT=3 + CEN=1 at 50 MHz … no stall-cycle data anywhere -> must measure»; GAPS: «Whether the 48 MHz target with LAT=1 is safe: vendor code uses LAT=3 … unverified»; erratum №5: «FPU fdiv.s/fsqrt.s corrupt results when operands are fetched from FLASH … a real flash-path pipeline hazard on this die»; §4/§1.17: RabbitRabbit needed WS=2 at 60 MHz for stability; research_errata §4: «РП ADCSD register map is wrong/stale» — РП tables demonstrably err.

**Фикс:** Add to P1 a flash read-integrity stress at LAT=1/48 MHz (repeated checksum of a large flash region, long duration, voltage/temp corners if available) as a G1 precondition; add risk row: trigger = integrity/soak errors, fallback = LAT=2 + full P1 re-run (state explicitly that all P1 numbers are LAT-specific).

### ?-8 [MINOR]
**Претензия:** Internal contradiction: G1's flash-first verdict rests entirely on MEASURED behavior of undocumented microarchitecture (CEN semantics, 2KB I-cache) on one physical specimen, yet §7.3 declares chip marking «не критично для битбанга». Undocumented behavior is exactly what may vary across silicon revisions (two USB-block revisions already exist; part-to-part margin differences observed), and CEN=0 cannot deliver the 'фиксированный prefetch' determinism hoped for in the fact base, because the РП prefetcher has no disable bit at all.

**Доказательство:** PLAN §7.3 vs Р4/G1 «CEN — по результату P1». research_flash.md §1: «What CEN gates … is stated NOWHERE», РП CTRL has «LAT[18:16] only»; §3: «no documented way to disable the prefetcher»; GAPS 1,3. shared_chat_notes.md: «при выключенном/фиксированном prefetch тайминг медленнее, но детерминирован» — premise without a hardware knob. research_errata.md: errata distinguish «2024 rev» vs «2025 rev с EP4»; p.12: 60 MHz overclock OK on one '2514', fails on another.

**Фикс:** Make recording the chip marking a P0 exit item (not an optional open question); add a risk row: 'G1 result is valid only for the measured die/revision — re-run G1 benches on any other marking/revision'; drop the expectation that CEN=0 freezes prefetch.

### ?-9 [MINOR]
**Претензия:** G1's headline measurement (P1.5 per-slot jitter histogram at ±1–2 cycle resolution, 20.8 ns @48 MHz) requires a logic analyzer sampling well above 100 MHz — but LA availability is still an unresolved open question, and the in-band alternative (rdcycle inside the emulated slot) self-perturbs the measurement because CSR accesses drain the pipeline, which the plan's own F3 acknowledges by banning CSR from the bit path.

**Доказательство:** PLAN §7.2: «Какой JTAG-адаптер и есть ли логический анализатор? (P1 методика: rdcycle + LA)» — open; P1.5: «LA на выходном пине: гистограмма периодов». research_core_irq.md §1: «CSR-access instructions 'исполняются в пустом конвейере'» (manual.txt:2168-2169); PLAN F3: «CSR только вне побитового пути».

**Фикс:** Gate P0 on confirming an LA with ≥100–200 MS/s (or define a substitute: timer input-capture / SPI-sniffer trick); in P1.5 forbid per-slot rdcycle — only aggregate rdcycle over N slots plus LA edge timestamps.

## Линза: ?

**Вердикт:** The plan's measurement-first skeleton is sound and most of its raw arithmetic checks out against the source (32 cyc/bit, SYNC 8×32=256, 48000/ms keepalive with modular-sub wrap already handled, 300 ns ≈ 14 cycles, −4..+6 correctly quoted from S:186-188, claim-deferred-to-tail PLIC usage is legal per the gateway semantics and adds nothing to the ≤7.5-bit turnaround). But through the cycle-timing lens it fails in the places where it is most confident: z3's "BSHR→DATAOUTTGL" is a category error — toggle cannot produce SE0, the J-park, or the initial K, and the substitution silently changes instruction counts inside cycle-counted TX slots (blocker if executed as written); z4's "предустановка DATAOUT" walks into a DPU-clobber on the shared port and drops the deliberate 2 MHz slew limiting that LS USB edge-rate compliance depends on; G1's acceptance math double-books the single −4..+6 total-displacement window between "drift" and "jitter" and measures it with a synthetic loop that structurally cannot miss the prefetch buffers the way the real slot code does; the "константная часть неважна" doctrine for IRQ entry contradicts the source's own "MUST check SE0 immediately" given the 64-cycle keep-alive SE0 window; and Р7's threshold scheme both contradicts z5's user-IRQ hook and misattributes non-preemption to the gateway. None of this invalidates the phase structure, but G1/P1 need the added gates and bench fixes before they can be trusted to make the flash-vs-TCM call, and z3/z4 need per-site register mappings rather than one-line substitutions.

### ?-1 [BLOCKER]
**Претензия:** Р2-z3 claims TX is 'запись BSHR → запись DATAOUTTGL (NRZI = toggle по нулевому биту: одна store)'. Toggle-only cannot express 3 of the 6 BSHR write sites: the initial K preset before output enable, the SE0/EOP (both lines driven LOW — an absolute state unreachable by a fixed toggle mask because the NRZI end state is data-dependent), and the J-park before release. Executed as written, no packet ever gets a valid EOP and enumeration fails.

**Доказательство:** /home/user/rv003usb/rv003usb/rv003usb.S: S:767-768 preset 'li s1, (1<<USB_PIN_DP) | (1<<(USB_PIN_DM+16)); c.sw s1, BSHR' (absolute K before CFGLR switch at S:771); S:992-993 SE0 'li s1, (1<<(USB_PIN_DM+16)) | (1<<(USB_PIN_DP+16)); c.sw s1, BSHR' (both-clear = absolute); S:997-998 J-park 'li s1, (1<<(USB_PIN_DM)) | (1<<(USB_PIN_DP+16))' (absolute). Only S:837 (preamble loop), S:948 (send_zero_bit) and S:1024 (insert_stuffed_bit) are toggle-expressible. Additionally the data-loop one-bit path (S:914-927) contains NO store at all — z3's 'одна store, значение = 0 или D±-маска' describes the preamble loop (unconditional store, S:837) but not the data loop, so the substitution changes instruction counts inside cycle-counted slots (xor s1,t1 disappears from S:947; a store may be added to the one-path), not just a register address.

**Фикс:** Respecify z3 per-site: bit-loop stores → DATAOUTTGL (constant D±-mask on the zero path, or 0/mask if made unconditional); SE0 → single DATAOUTCLR=D±mask; SE0→J → single DATAOUTSET=DM-mask; preset K → DATAOUTSET/DATAOUTCLR pair (or TGL only with a guaranteed J parked in DATAOUT, incl. first-ever TX init). Explicitly note that TX slot padding (z7) must re-balance the changed instruction sequence, not just swap the store target.

### ?-2 [MAJOR]
**Претензия:** Gate G1 (§4 P1) acceptance math is wrong: 'per-slot джиттер ≤±2 тактов И суммарный дрейф ... в окне −4..+6' treats jitter and drift as separate budgets, but S:186-188 defines −4..+6 as the TOTAL sampling-phase displacement tolerance. Drift at +6 plus jitter +2 puts a sample at +8, outside the measured window — the gate can green-light a failing flash configuration. Also, in a software-timed loop per-slot deviations are cumulative (a +1 stall shifts every later sample), so the criterion must bound the running sum at every bit, not endpoint drift plus an independent per-slot band.

**Доказательство:** /home/user/rv003usb/rv003usb/rv003usb.S:186-188: '8 extra cycles here cause errors. -5 cycles is too much. -4 to +6 cycles is OK' — a single static-offset window at done_preamble affecting all subsequent samples. PLAN.md §4 G1: 'per-slot джиттер ≤±2 тактов и суммарный дрейф на 90-битном пакете в окне −4..+6 → флеш'. No fact-base source assigns jitter a budget outside the −4..+6 total.

**Фикс:** Rewrite G1 criterion as: max cumulative sample-point displacement over the whole packet (running sum of per-slot period errors from bench-5 histogram, evaluated at every bit, worst path) must lie inside −4..+6 with margin reserved for host clock tolerance; drop the separate ±2 per-slot figure or derive it so that drift+jitter ⊂ [−4..+6].

### ?-3 [MAJOR]
**Претензия:** Р2-z4 'предустановка DATAOUT': a full-word DATAOUT write to preset the K state clobbers bit 2 of port C — the DPU pin (Р5: D+=C0, D−=C1, DPU=C2 on the SAME port). DPU drops low → the 1.5k pull-up on D− becomes a pull-down → host sees a disconnect on the first IN response. The claimed 1-store preset is not achievable with a plain DATAOUT write; the safe preset is a SET/CLR pair (2 stores) or TGL with a guaranteed prior J state.

**Доказательство:** PLAN.md Р5 puts DPU on C2, same port as D±; research_gpio.md §1: 'DATAOUT: read returns state; write drives pins (manual.txt:16874-16884)' — full 16-bit write, no masking. rv003usb never touches whole-port output state: the V003 preset uses BSHR set/clear semantics touching only D± (/home/user/rv003usb/rv003usb/rv003usb.S:767-768), and preset strictly precedes direction switch (S:768 before S:771). The plan states neither the register form nor the preset-before-OUTENSET ordering.

**Фикс:** Specify z4 as: DATAOUTSET=DP-mask + DATAOUTCLR=DM-mask (or DATAOUTTGL from parked J), never full DATAOUT; keep preset-before-OUTENSET ordering; alternatively use MASKLB masked write (documented for writes, research_gpio.md §1). Update the 'короче, чем у V003' instruction count to ~3 stores total (still shorter).

### ?-4 [MAJOR]
**Претензия:** Р2-z4 silently drops a load-bearing electrical property of the CH32 CFGLR sequence: V003 deliberately configures D± as 2 MHz slew-limited outputs (LS USB requires 75–300 ns rise/fall). K1921VG015 has no drive-strength/slew register at all, so OUTENSET enables full-speed pads; TX edge-rate compliance is nowhere in the plan. R5 covers only the RX direction (slow incoming edges without Schmitt), not TX signal quality, and nothing triggers measuring TX edges.

**Доказательство:** /home/user/rv003usb/rv003usb/rv003usb.S:759-764: '// Convert D+/D- into 2MHz outputs ... li s1, ((0b0010<<(USB_PIN_DP*4)) | ...' — the mode nibble is chosen for slew, not just direction. research_gpio.md §1: 'DRIVEMODE / drive strength: NO such register ... drive-strength control undocumented/absent' and GAPS 5-6 (no slew, no max toggle frequency spec); §5: only 4 mA drive, CL ≤ 40 pF spec. PLAN.md §3/R5 discuss only slow RX edges ('медленные фронты LS ... дребезжать на одиночных сэмплах').

**Фикс:** Add to P4: scope/LA measurement of TX D± rise/fall at the connector against the 75–300 ns LS envelope; pre-plan the mitigation (series R / RC on D±, same components as R5's RX fix) so the board provides footprints from P0.

### ?-5 [MAJOR]
**Претензия:** §3 and P1.3 declare the constant part of IRQ entry latency irrelevant ('важен разброс, не абсолют'; 'константная часть неважна'). False: the SE0-keepalive classification is made by the FIRST D− sample (5th instruction), and a keep-alive is an SE0 of only 2 bit-times = 64 cycles @48 MHz. Constant entry latency (2-clk input sync + IGW + trap entry + 4 instructions) must land the sample inside that window, or every keepalive is misclassified as a packet start; the source itself flags this as a hard constraint. No P1 gate checks the measured median against this bound.

**Доказательство:** /home/user/rv003usb/rv003usb/rv003usb.S:69: 'c.lw a0, INDR_OFFSET(a5) // MUST check SE0 immediately.' and the SE0-vs-packet branch at S:91-100 uses only that first sample. PLAN.md §3: 'вход в IRQ: измерить разброс (константная часть неважна)'; §4 P1.3 collects 'медиана + разброс' but G1/R2 gate only on spread ('Разброс входа в IRQ > edge-catcher'). research_core_irq.md GAPS 2: trap-entry latency wholly undocumented — it could be large.

**Фикс:** Add an explicit P1.3 gate: median(edge→first-instruction) + 4 instructions + GPIO lw cost + 2-clk sync must be ≲55 cycles (keep-alive SE0 = 64 cycles minus sampling margin); if violated, the head stub in TCM-A (Р7) or restructured SE0 detection becomes mandatory regardless of jitter results.

### ?-6 [MAJOR]
**Претензия:** Р7 'PLIC-приоритет: только наш источник > threshold' permanently disables every non-GPIO interrupt on the chip (sources with priority ≤ threshold never assert EIP), which contradicts Р2-z5's user-IRQ hook 'не-наш источник → бранч на слабый символ' — under Р7 no non-GPIO source can ever reach the handler. It also misattributes non-preemption to the gateway: F5's gateway guarantee is same-source-only; protection from OTHER sources during the USB ISR comes from mstatus.MIE=0 in the trap, plus the ≤40-cycle user-handler contract.

**Доказательство:** research_core_irq.md §4: 'EIP asserted only if pending priority strictly > threshold — manual.txt:2410-2416' and gateway text limited to 'запроса прерывания из того же источника' (manual.txt:2507-2509). PLAN.md z5 promises a user hook 'по образцу RV003_ADD_EXTI_HANDLER' — but on V003 that hook shares one EXTI vector while other peripherals keep their own PFIC vectors (stack_portability.md §5); under Р7's threshold rule K1921 UART/TMR/DMA IRQs are dead chip-wide. README contract per stack_portability.md §5: 'must never be preempted… other critical sections ≤ ~40 cycles'.

**Фикс:** Change Р7 to: GPIO source at the highest priority, other needed sources at lower priorities but ABOVE threshold; non-preemption of the USB ISR is guaranteed by mstatus.MIE=0 during the trap (never re-enable MIE inside), gateway only prevents same-source re-entry; keep the ≤40-cycle rule for all other handlers. z5's weak-symbol hook then dispatches by MICC claim value for non-GPIO sources.

### ?-7 [MAJOR]
**Претензия:** P1 (and the F12 'неизмерено' list) omit the PLIC MICC claim/complete access cost and the full ISR-exit-tail + re-entry cost. These sit in a bounded real-time window the plan never budgets: after the device's IN-data EOP, the handler must restore registers, clear INTSTATUS, claim+complete MICC, mret, then re-enter the trap fast enough to catch the host's ACK (a separate packet needing a fresh ISR entry, ~2 bit-times gap + 8-bit SYNC ≈ 300 cycles total). MICC access cost is an explicitly documented unknown; a slow system-bus MMR access would surface only as flaky lost ACKs in P4 — expensive to diagnose there.

**Доказательство:** research_core_irq.md §4: 'Minimal ISR cost = 1 load + 1 store to 0x0C20_0004 (system bus / MMR space; РП does not specify per-access cycle cost → GAPS)' and GAPS 3: 'bus arbitration cost for AHB/MMR accesses (incl. PLIC MICC claim read cost): not documented'. PLAN.md F12 lists only 'цена lw из GPIO, вход в IRQ, taken branch, fetch-стойлы, детерминизм I-cache'; P1 benches 1-5 contain no MICC/tail bench. ACK reception requires re-entry: usb_pid_handle_ack path /home/user/rv003usb/rv003usb/rv003usb.S:608-626 with the exit tail at S:544-590. R9 covers only the device→host turnaround (>7.5 bt → ACK-first), not the exit/re-entry window.

**Фикс:** Add P1 bench 6: rdcycle-pair around lw/sw of MICC (0x0C200004) and INTSTATUS, plus a measured edge→edge back-to-back IRQ test (fire, exit with full tail, re-fire immediately; LA the gap). Budget check: tail + entry < ~200 cycles.

### ?-8 [MAJOR]
**Претензия:** P1.5 ('синтетический 32-тактный цикл (load-DATA + работа + store-TGL)') cannot answer the question G1 asks. A tight synthetic loop fits entirely in the 2×128-bit prefetch buffers and will always look deterministic from flash. The real RX slot alternates every bit between code paths far larger than 32 bytes apart (bit_process → handle_one_bit/handle_zero_bit → is_end_of_byte → back), guaranteeing a buffer miss on nearly every bit; flash-first viability then rests entirely on the UNDOCUMENTED 2K I-cache, which bench 5 as specified never exercises. G1 would produce a false pass.

**Доказательство:** research_flash.md §3: hit only 'within the current + next 16-byte line'; 'a miss (taken branch outside the two buffers) costs a full flash access'; §1: I-cache '2 КБайт' with 'no I-cache control/bypass mechanism' documented. Real topology: /home/user/rv003usb/rv003usb/rv003usb.S bit_process S:304, handle_one_bit S:367, is_end_of_byte S:298, se0_complete S:427 — the per-bit round trip spans hundreds of bytes. PLAN.md P1.5 specifies only a synthetic cycle; H1 ('горячий ISR ~1.2К целиком садится в I-cache') is exactly what a small synthetic loop cannot test.

**Фикс:** Respecify P1.5: run the actual RX bit engine (or a replica preserving the real branch distances and code footprint, incl. cold-cache first iteration) against a synthetic edge pattern, from flash and TCM; record per-bit period histogram AND first-packet (cold I-cache) vs repeat-packet behavior — this directly tests H1/H2 instead of the prefetch buffers.

### ?-9 [MINOR]
**Претензия:** G1's drift budget is computed over a '90-битный пакет', which ignores bit stuffing: worst case after the preamble is PID 8 + data 64 + CRC16 16 = 88 bits plus up to ~14 stuffed bits ≈ 102 bit slots. The drift window is ~13% tighter than the gate assumes.

**Доказательство:** PLAN.md §4 G1: 'суммарный дрейф на 90-битном пакете'. Bit stuffing is in the engine: /home/user/rv003usb/rv003usb/rv003usb.S:250-254 (stuff counter), S:396-424 (handle_bit_stuff consumes a full extra slot); USB_BUFFER_SIZE-based max payload per stack_portability.md §1. shared_chat_notes.md's '~90 бит' was an estimate, not a worst case.

**Фикс:** Use ~102 slots (88 + worst-case stuffing) in the G1 drift computation; one-line change to the gate.



# ROUND 2 (по PLAN v2.2): линзы arch / boot / complete

## Линза: boot

**Вердикт:** Through the bootloader/flash-layout lens, v2.2's Р8 is the plan's least-cooked section and says so itself ('Детали ревизует линза boot'), but the gaps are not mere details: the entry side is specified while the exit side rests on a V003 mechanism (boot-mode bit + PFIC reset, boot.c:110-115) that physically does not exist on WG015 — 'как v1' cannot be executed for the app handoff, and the state contract (mtvec/PLIC/INTSTATUS/MIE/DPU/clocks) handed to the app is undefined (blocker). Two structural consequences of moving from a protected 1920 B boot zone to page 0 of unified 1 MB flash are unaddressed: nothing stops a blob from erasing the loader itself (only SERVEN+JTAG recovers), and 4 KB-page erase creates an undocumented-length (100 ms-scale timebase) USB dead window that the host CLI must pace around — the adopted busbflash pattern has a blind 20 ms sleep and no retry. The parts that are covered are covered well: the 4 KB size budget is realistic (V003 fits ~1908 B on RV32EC with size scavenging; WG015 adds roughly 1-1.5 KB of PLL/PLIC/startup/far-addressing overhead) and R8's link-time 8 K escalation is a cheap, adequately-placed gate; R6 (16/64 B write unit) and R7 (IRQ-off during BUSY) are correctly parameterized; OpenOCD DCVostok flash support is verified in the fact base; and the 120 B blob limit is a non-issue (scratchpad is 1152 B with a streaming protocol). Remaining minors are literal-transfer traps: the SECRET XOR encoding assumes 16-bit link addresses, the timeout is a loop-count calibration not a timer, RTC_REG[0] needs clear-on-read + RSTSTAT qualification, and the host CLI's real reference (minichlink's blob flow) lives in an empty submodule. Fix the exit mechanism, the self-erase guard, and the erase-window pacing in Р8/P5 before P5 starts; the rest of the plan's bootloader scaffolding is sound.

### boot-1 [BLOCKER]
**Претензия:** Р8 («Бутлоадер. Как v1 ... вход: RTC_REG[0] + RSTSYS + таймаут/кнопка») specifies only the ENTRY path and silently assumes the V003 exit-to-app mechanism ports, but that mechanism is a hardware boot-mode switch + full chip reset that has no equivalent on WG015. 'Как v1' is unimplementable for the boot switch: on V003 boot_usercode does FLASH->BOOT_MODEKEYR unlock, FLASH->STATR=0 (bit14 selects image) and PFIC->SCTLR=1<<31 — a reset after which HARDWARE starts the user image from a clean state. On WG015 the bootloader owns the reset vector at 0x8000_0000 with no ROM loader and no mode bit; a plain RSTSYS resets back INTO the bootloader, and a jump-to-app hands the app live bootloader state: mtvec pointing into bootloader flash (Р7 direct handler), GPIO source enabled in PLIC MIEM0 with possibly pending INTSTATUS, mstatus.MIE=1, DPU=C2 still driving high (host still sees the bootloader device enumerated — the app cannot re-enumerate without a DPU cycle), PLL/LAT configured. The plan defines none of this handoff contract, nor the flag protocol (RTC_REG 'skip-bootloader' + RSTSYS early-out) that replaces the V003 reset trick.

**Доказательство:** bootloader/bootloader.c:110-115: 'FLASH->BOOT_MODEKEYR = FLASH_KEY1/2; FLASH->STATR = 0; // 1<<14 is zero, so, boot user code. PFIC->SCTLR = 1<<31;' — exit is a reset, not a jump. stack_portability.md §8 item 10: 'K1921 has no separate boot zone and no ROM loader → bootloader at flash base, jump-to-app instead of option-byte/BOOT_MODEKEYR/PFIC-reset switching'. research_usb_power.md §3: reset PC = flash base 0x8000_0000, RSTSYS = RCU+C0h KEY A55A (resets to bootloader). DPU handoff need: bootloader.c:253-260 drives DPU high; the no-DPU variant must force a 1 s D− disconnect pulse before booting (bootloader.c:105-108). PLAN.md Р8 (lines 138-141) and §7 defaults contain no exit/handoff item at all.

**Фикс:** Add to Р8 an explicit exit-to-app design: (a) mechanism = RTC_REG[0] two-valued flag ('stay in loader' / 'boot app') + RSTSYS, with a fast path in the bootloader startup that, on the 'boot app' flag, clears the flag and jumps to app before enabling any USB/PLIC/DPU state (near-reset state guaranteed); (b) written state contract for the jump: mtvec, MIEM0/threshold, INTSTATUS cleared, MICC completed, mstatus.MIE, DPU released (disconnect) — and whether app receives 48 MHz PLL or reset clocks; (c) app-side reboot-to-bootloader = write RTC_REG[0] + RSTSYS (replacing rv003usb.c:179-184 WCH sequence).

### boot-2 [MAJOR]
**Претензия:** Nothing protects the bootloader's own flash page from the erase/write blobs — a hole that did not exist on V003 and that the plan (Р8, P5, §5, §7) never mentions. On V003 the loader lives in a physically separate 1920 B boot zone at 0x1FFFF000, so user-flash blobs could not touch it ('you can't update the bootloader using itself'). On WG015 the loader is page 0 of the same 1 MB main flash; blobs are arbitrary host-supplied code, the flash controller erases any page given ADDR in page 0, and CMD.ALLSEC is one bit away from full-chip erase; the only write protection (CFGWORD.FLASHWE) is global — it would also block app flashing — and recovery from a self-erase is JTAG + SERVEN service mode only, defeating the '100 циклов reflash без JTAG' P5 criterion.

**Доказательство:** bootloader/ch32v003fun-usb-bootloader.ld:5-6: 'Actually at 0x1FFFF000 but the system maps it to 0x00000000' — separate boot zone. bootloader/README.md:7: 'you can't update the bootloader using itself'. research_flash.md §4: erase 'page (ERSEC, page from ADDR) or full area (ALLSEC+ERSEC)'; 'Write protection: CFGWORD.FLASHWE=0 turns register writes into reads (manual.txt:2103-2111)' — global, no per-page protect. research_usb_power.md §4: SERVEN mass-erase is the unbrick path. PLAN.md Р8/P5/§5: no guard mentioned; риск-таблица has no self-erase row.

**Фикс:** Add one rule to Р8: every erase/write blob begins with an address guard (reject ADDR < 0x8000_1000, never set ALLSEC/NVRON) — ~4 instructions in the blob — and the host CLI independently refuses addresses below the app base; add a risk row (trigger = bad blob/CLI bug, consequence = SERVEN+JTAG recovery).

### boot-3 [MAJOR]
**Претензия:** The 4 KB-page erase creates a multi-ms USB dead window (IRQ off + flash reads garbage) that is a qualitatively different regime from the V003 precedent, and the plan says nothing about host-side pacing or measuring the erase time. Erase duration is undocumented, but the controller's erase timebase register is 100 ms-scale (TERSR = 'clk cycles per 100 ms', reset 10^7), so tens-to-hundreds of ms per page is plausible; during that window the device answers nothing (mstatus.MIE=0 per Р8/R7, and any flash fetch returns garbage), so any in-flight LS control transfer fails after the host's ~3 transaction retries (few ms). The only in-repo host pattern the plan adopts (busbflash) uses a single blind usleep(20000) with no retry loop — the '100 циклов reflash' P5 criterion would fail flakily with no attribution.

**Доказательство:** research_flash.md §1 table: 'TERSR | +24h | 10000000 | clk cycles per 100 ms (erase timebase)'; §4: pages are 4 KB ('256 pages × 4 KB'), 'Absolute t_prog/t_erase (typ/max µs/ms) are NOT stated in РП' (GAP 6); 'fetches during program/erase return garbage' (manual.txt:2016-2018). bootloader/testblink/busbflash.c:41: 'usleep( 20000 );' — fixed delay, no retry; hid_get_feature_report follows unconditionally (busbflash.c:44). PLAN.md Р8/R7 cover only device-side IRQ-off; P5 ('Первый блоб — тест юнита записи 16/64 Б') has no erase-time measurement or CLI pacing step.

**Фикс:** Extend P5: (a) first blobs also measure real page-erase and 16-byte-program times (rdcycle in blob, value written to scratchpad for host readback); (b) CLI protocol rule: never have a transfer in flight while a blob is executing — issue blob, wait measured-time + margin, then poll with a retry loop tolerating failed control transfers; (c) state that per-page erase, not whole-app erase, is done between report exchanges so dead windows stay bounded.

### boot-4 [MINOR]
**Претензия:** Р8/§7 fix the host CLI as 'hidapi, busbflash-паттерн; minichlink не трогаем', but busbflash.c is a 64-line single-report smoke test, not a flasher: it uploads one ≤124 B blob (128 B report minus magic trailer) and reads it back; it contains no erase/write blob library, no multi-report streaming (the device-side continuation protocol via e->count and the magic scan at start+4 exists but has no in-repo host reference), no SECRET decode, no reboot-to-app — and it even #includes hidapi.c from the minichlink directory of the ch32fun submodule, which is EMPTY in this repo. The real host reference (minichlink -B flow with its CH32 blob set) is unavailable in the fact base, so P5 implicitly contains an unplanned 'design the full blob set + streaming/pacing protocol from the device side' work item. Note also the 120 B blob limit is a per-report framing convention, not a device constraint (scratchpad is 1024+128 B and continuation is supported) — WG015 blobs (ADDR/DATA0..3/CMD ~20 instr + TACCR..TPGSR rescale ~7 stores + mstatus save/restore) fit comfortably, so no size risk, but the plan never states this either.

**Доказательство:** bootloader/testblink/busbflash.c:26-44 (whole flow: fread ≤124 B, one hid_send_feature_report, one readback; ':5' includes "hidapi.c"; Makefile:12 '-I../../ch32fun/minichlink'). stack_portability.md §1: 'ch32fun/ submodule EMPTY (git submodule status: -1e4887e)', 'minichlink flasher (bootloader/Makefile:13)'. Device-side streaming: bootloader.c:453-471 (continuing data, magic 0x1234abcd scan, e->count limit SCRATCHPAD_SIZE/8), :68 'SCRATCHPAD_SIZE 1024+128'. PLAN.md §7: 'Хост-CLI | hidapi, busbflash-паттерн; minichlink не трогаем'.

**Фикс:** In P5, add an explicit first step 'specify the wire protocol from the device side (report framing, continuation, magic trailer, readback, SECRET) and enumerate the blob set (rescale-timings, erase-page, program-chunk, boot-app)'; note that busbflash covers only single-report framing and that blob size is bounded by the 1152 B scratchpad, not 120 B.

### boot-5 [MINOR]
**Претензия:** Р8's 'SECRET-механика с новыми адресами' is wrong as worded: the V003 SECRET encoding is not relocatable by 'new addresses' because it packs the full address into 16 bits. ld:25 computes ((addr XOR 0x77C)<<16)|addr, which is invertible/verifiable only when addr < 0x10000 — true on V003 because the boot zone links at 0x00000000 and boot_usercode sits at ~0x7xx. Linked at 0x8000_0xxx the XOR high-half truncates and the low OR collides with it, producing an undecodable token; carrying the mechanism over with merely 'new addresses' fails silently at P5 when the host CLI tries to locate/verify boot_usercode.

**Доказательство:** bootloader/ch32v003fun-usb-bootloader.ld:25: '_boot_firmware_xor = ABSOLUTE((((ABSOLUTE(.)|0x77C) - (ABSOLUTE(.)&0x77C))<<16)|ABSOLUTE(.))' ((a|b)-(a&b) = a^b); ld:5-6: flash 'mapped to 0x00000000', LENGTH 1916 — addresses fit 16 bits; bootloader.c:91-92: secret_xor = (uint32_t)(&_boot_firmware_xor) stored at fixed .secret_address (ld:7 ORIGIN 0x77C). WG015 flash base 0x8000_0000 (research_flash.md §5). PLAN.md Р8: 'SECRET-механика с новыми адресами'.

**Фикс:** Respecify: SECRET stores the OFFSET of boot_usercode from 0x8000_0000 (fits 16 bits for a 4-8 K loader) with the same XOR-fold as integrity check, at a new fixed offset; host decode adds the base. One line in Р8 plus the ld formula.

### boot-6 [MINOR]
**Претензия:** F8/Р8's RTC_REG[0] boot flag needs a staleness rule the plan lacks: RTC_REG survives far more than the RSTSYS soft reset — per РП it clears only after removal of power on BOTH VCC1 and V_BAT, so on boards with V_BAT wired a stale 'enter bootloader' (or, after fixing finding 1, 'boot app') flag survives full power cycles and misdirects the next boot; the battery-domain question is itself contested in the fact base (makc: 'нет батарейного домена' vs РП/kenny5660), i.e. the survival envelope is board/revision-dependent. V003 handled the analogous problem by qualifying with an exact RCC->RSTSCKR == 0x10000000 reset-cause check; the plan carries no clear-on-read rule and no RCU.RSTSTAT (POR/SYSRST) qualification.

**Доказательство:** research_usb_power.md §3: PMURTC regs 'сбрасываются только при включении микроконтроллера после полного отключения питания … по выводам VCC1 и V_BAT' (manual.txt:7169-7171); RCU.RSTSTAT SYSRST/WDT/POR bits (manual.txt:14916-14925). research_errata.md:63 vs :76/:125 — battery-domain contradiction 'unresolved'. V003 precedent: bootloader.c:240-246 'RCC->RSTSCKR == 0x10000000' exact-match qualification. PLAN.md F8: only 'флаг «в бутлоадер» в RTC_REG[0]' (and errata №1 RTC_REG[14] avoidance).

**Фикс:** Add two lines to Р8: bootloader reads RTC_REG[0] and immediately clears it (flag is one-shot), and honors it only when RCU.RSTSTAT indicates SYSRST (not POR); works identically whether or not a real battery domain exists.

### boot-7 [MINOR]
**Претензия:** The bootloader entry timeout does not use SysTick at all — it is a busy-loop iteration counter whose '75 ms per unit' constant is calibrated to V003 loop speed and even chosen for code size ('Constant works out to a single lbu instruction') — so z6's 'SysTick CNT → rdcycle' swap (which addresses only the asm keepalive) leaves the C-side timeout un-ported: on WG015 the per-iteration cost (RV32IMC, flash prefetch/I-cache, different ISR load) is unknown until P1, and the user-visible 5 s window scales by an arbitrary factor. No plan step re-derives it.

**Доказательство:** bootloader/bootloader.c:270-298: 'int32_t localpad = BOOTLOADER_TIMEOUT_BASE * BOOTLOADER_TIMEOUT_PWR; while(1) { ... if( ++localpad == 0 ) boot_usercode(); ...' — pure iteration counting, no timer read; :49-51: '75ms per unit', 'Careful: Constant works out to a single lbu instruction'; SysTick (boot.c:121 'SysTick->CTLR = 5') feeds only the .S keepalive (stack_portability.md §3: SysTick CNT read S:677-683). PLAN.md z6 covers only 'keepalive: SysTick CNT → rdcycle'; Р8 says just 'таймаут/кнопка'.

**Фикс:** One line in Р8: timeout on WG015 = rdcycle delta in the main loop (threshold in cycles at 48 MHz), replacing the iteration-count calibration; if the size-optimized single-load trick is kept, recalibrate the constant against the measured P1 loop cost.

## Линза: arch

**Вердикт:** v2.2 is genuinely stronger where the first panel hit it (timing, G1, flash), but through the architecture lens its confidence outruns its specification in four places. One feature is self-defeating: LOCKSET on D± would block the very DATAOUT*/OUTEN* writes that z3/z4 perform every packet (research_gpio §11.6 — locked pins accept only INTSTATUS/QUALSAMPLE writes), so a 'hardening' line item is an enumeration-killing blocker. The Р2 centerpiece — same-file zoning with a 'under ~30% #if' fork gate — is measured against a file that already carries 27.5% conditional lines for two targets, with the metric undefined and the decision scheduled at end of P3, before the heaviest zones (z3/z4 TX, z8, tiny-boot analog, Р10 markers) exist; and the mechanism implied by the metric (inline per-site chip #if) is the exact anti-pattern the plan's own branch_notes records, which quietly forfeits the stated 'third target without rework' goal that per-site named macros (the VOOXDELAY mechanism, generalized) would keep. The seam story is real but under-specified: the ch32fun.h hard-includes, the FUNCONF_SYSTICK_USE_HCLK #error, the XW_C_* macro sites (5, one inside the counted TX loop — absent from every zone), per-demo funconfig/usb_config conditioning, and Makefile.wg015's replication of ch32fun.mk are all unenumerated and will be improvised mid-P2. Р10's DPU-as-DBG0 contradicts the plan's own F10 electrics (≈200 mV injection, not 'десятки мВ') and trades a free reserve pin for a permanent re-park correctness invariant. On the positive side: the bit-identity gate is achievable with the macro discipline (extend it to the V00x variant), z2/z6/z7 are genuinely cheap, the layered keepalive-servo (generic measurement + per-target actuator) is the right KISS-compatible answer to the crystal-less question, defaults-not-questions §7 is good process, and R10's process claims match the observed commit/STATE.md discipline. Fix the LOCKSET contradiction, redefine and re-time the fork gate, specify the seam contract including XW, and the architecture is sound.

### arch-1 [BLOCKER]
**Претензия:** Р5 «LOCKSET на D±-конфиг» is self-contradictory with the plan's own z3/z4: on this GPIO block a locked pin accepts writes ONLY to INTSTATUS and QUALSAMPLE, so every TX operation the plan specifies on D± — DATAOUTSET/CLR/TGL bit stores (z3 table) and the OUTENSET/OUTENCLR turnaround (z4) — is blocked while the lock is on. Executed as written, no packet can ever be transmitted; the only escape is LOCKKEY unlock/relock churn inside the cycle-critical path, which the plan nowhere budgets. This is over-engineered 'hardening' that buys protection against a clobber the code itself must perform every packet.

**Доказательство:** research_gpio.md §1 (manual.txt:4101-4116): «LOCKKEY/LOCKSET/LOCKCLR: config lock; … when locked, only INTSTATUS and QUALSAMPLE remain writable for locked pins». PLAN.md Р5: «LOCKSET на D±-конфиг»; PLAN.md z4: «OUTENSET=D±mask (захват) … OUTENCLR=D±mask (отпуск)» per packet; z3 table: DATAOUTTGL/SET/CLR stores on D± per bit. Also Р10 markers store to DATAOUTTGL on the same port.

**Фикс:** Delete LOCKSET from Р5 (or restrict it to pins the stack never writes, e.g. ALTFUNC config of unrelated pins). One-line change; nothing else depends on it.

### arch-2 [MAJOR]
**Претензия:** Р2's fork-vs-parameterize gate is both ill-defined and mistimed. (a) The metric «>~30% строк под #if» is nearly consumed before the port starts: rv003usb.S already has 300 of 1092 lines (27.5%) inside preprocessor conditionals for just the two existing targets (V003/V00x), and the plan never says whether the 30% counts total conditional lines or only new WG015 ones. (b) The decision is fixed «в конце P3» (P3: «Фиксация решения Р2-отхода»), but the largest divergence contributors — z3 per-site TX stores with changed instruction sequences («не только адрес store»), z4 turnaround blocks, z8 3-way dispatch trampolines, the WG015 tiny-boot analog (Р8 «свой tiny-boot»; V003's USE_TINY_BOOT is ~60 lines inside rv003usb.S:1032-1092), and Р10 marker stores that exist only in WG015 slot ledgers — are all implemented in P4/P5 or on R1-fail. The gate therefore decides on the smallest half of the diff surface.

**Доказательство:** Measured: `awk` over /home/user/rv003usb/rv003usb/rv003usb.S → 300/1092 lines inside #if/#endif; 75 preprocessor lines; 64 VOOXDELAY sites. PLAN.md:48-49 «форк-копия при >~30% строк под #if, решение в конце P3»; PLAN.md P3 (line 225) «Фиксация решения Р2-отхода» vs P4 (line 227) «Зоны z3/z4 (по-сайтовой таблице)»; z3 note (line 65-66) «z7 перебалансирует изменившиеся последовательности инструкций … (не только адрес store)».

**Фикс:** Define the metric as NEW WG015-conditional lines / total, state the number, and move the fork decision to end of P4 (after z3/z4 exist) — or pre-commit by estimating the P4 zones on paper at P3 and recording both numbers in the decision note.

### arch-3 [MAJOR]
**Претензия:** The zone list z1–z8 (presented as the complete .S diff surface) omits the WCH XW custom-instruction sites entirely: XW_C_LBU/XW_C_LHU/XW_C_SB at 5 always-compiled sites, one of them (S:890) inside the cycle-counted TX byte loop. BM-310S6 is RV32IMC — these encodings are illegal there. No zone, no seam, and no P2 step provides the standard-instruction replacement, so P2's exit criterion «скелет зелёный» cannot be met as written; and because lbu (32-bit) replaces a 16-bit XW op inside a counted loop, the z7 ledger and fetch alignment change — exactly the class of surprise the plan's own fact base flags as checklist item 7.

**Доказательство:** rv003usb.S:439 «XW_C_LBU(a0, a1, 0)», S:477, S:485, S:665, S:890 «XW_C_LBU(s0, a0, 0)» in load_next_byte of the TX loop; macros come from ch32fun.h (stack_portability.md §7: «5 sites total; the S:890 one sits inside the cycle-counted TX byte loop»). stack_portability.md §8 item 7: «Replace XW instructions (5 sites, §7) with standard lbu/lhu/sb; re-balance the affected TX loop timing (S:890)». PLAN.md Р2 zones z1-z8 and Р3 seams: no mention of XW anywhere (grep of PLAN.md = 0 hits).

**Фикс:** Add to Р2/Р3: usb_port_wg015.h defines XW_C_LBU/LHU/SB as standard lbu/lhu/sb for WG015 (V003 keeps ch32fun's); add the S:890 site to the z7 ledger and to the P1.5 TX-slot emulation (2→4-byte instruction changes fetch footprint).

### arch-4 [MAJOR]
**Претензия:** The extensibility promise «Не переделывать архитектуру при появлении третьего таргета» (§0) is not backed by the chosen mechanism. The metric «% строк под #if» presumes inline per-site chip conditionals — precisely the anti-pattern the plan's own fact base condemns; the file already carries ~22 CH32V00x conditional blocks, and z3/z4/z5/z6/z8 as specified (a WG015 column per site) turn them into 3-way ladders that a chip #4 makes 4-way, touching the shared .S body at every zone site. The z3 table is written as chip columns («Сайт V003 | Семантика | WG015»), not as abstract per-site primitives, so each new target edits every site rather than supplying one header.

**Доказательство:** branch_notes.md:83-85 (anti-patterns): «#if sprinkling per chip variant inside slot code (#if PY32F002Bx5 nops) — fragile; prefer a single DELAY/PAD macro parameterized per target (as master's nx6p3delay)». The VOOXDELAY precedent puts #if only at the definition block (rv003usb.S:18-26), uses unconditioned — the plan adopts this mechanism only for z7 padding, while z3/z4/z5 are specified as per-site substitutions. PLAN.md:13 «Не переделывать архитектуру при появлении третьего таргета»; z3 table header (PLAN.md:54).

**Фикс:** Respecify z3/z4/z5/z6 as named per-site macro contracts (e.g. USB_TX_PRESET, USB_TX_FLIP, USB_TX_SE0, USB_TX_PARK, USB_SAMPLE, USB_ISR_ACK, USB_TICK_READ, USB_TRIM_ACTUATE) defined once per target in usb_port_<chip>.h; the .S body references only macros. Then chip #4 = new header + Makefile + P1 rerun, and the fork metric becomes 'sites needing semantics a macro cannot express'.

### arch-5 [MAJOR]
**Претензия:** The Р3 seam «usb_port_wg015.h» does not slot into the actual include graph, and the plan never specifies the plumbing: rv003usb.S:1 and rv003usb.c:10 hard-include ch32fun.h (so either the shim impersonates that name or both includes go conditional — unstated); rv003usb.h:17-19 hard-#errors without FUNCONF_SYSTICK_USE_HCLK, which is meaningless on WG015 where z6 replaces SysTick with rdcycle — the guard must be neutralized per-target and no plan step does it; the shim symbol inventory is never enumerated (XW_C_* macros, TIM1_BASE — referenced by the always-compiled DEBUG_TICK macros S:30-38, SysTick struct, NVIC_EnableIRQ c:153, RCC/EXTI structs used by the c:55-154 init seam); each demo's funconfig.h/usb_config.h hardcodes CH32V003 and V003 pins, so P4's demo_hidapi build requires multi-target conditioning of per-demo configs (the py32 precedent pattern) that Р9 never mentions; and Makefile.wg015 must replicate ch32fun.mk's compile/link/objcopy-.bin/flash roles plus the bootloader's WRITE_SECTION/option-byte analog — none enumerated. All of this surfaces mid-P2 as ad-hoc decisions.

**Доказательство:** rv003usb.S:1 «#include "ch32fun.h"»; rv003usb.c:10 «#include "ch32fun.h"»; rv003usb.h:17-19 «#if !defined(FUNCONF_SYSTICK_USE_HCLK) … #error "RV003USB requires #define FUNCONF_SYSTICK_USE_HCLK 1"»; rv003usb.S:30-38 DEBUG_TICK_SETUP references TIM1_BASE in BOTH branches (dummy sink TIM1_BASE+0x58); bootloader/Makefile:13-15 (FLASH_COMMAND minichlink, include ch32fun.mk); bootloader/funconfig.h per stack_portability.md §4 «CH32V003 1 … FUNCONF_SYSTICK_USE_HCLK 1 (mandatory, h:17-19)». PLAN.md Р3/Р9 mention only «Три шва + usb_port_wg015.h» and «Makefile.wg015; … вендорим только K1921VG015.h; старт/ld свои».

**Фикс:** Add to P2 a one-paragraph seam spec: shim header name and include-graph position (recommend a wg015/ch32fun.h shim dir selected by -I, keeping S:1/c:10 untouched); the guard at h:17-19 becomes #if !TARGET_WG015; enumerate shim-provided symbols (XW_C_*, SysTick-replacement, NVIC_EnableIRQ, DEBUG sink address); list Makefile.wg015 targets (elf/bin/lst, flash via OpenOCD, bootloader section) and the per-demo config conditioning pattern.

### arch-6 [MAJOR]
**Претензия:** Р10's DBG0-on-DPU 'double role' contradicts the plan's own electrical fact and adds a correctness coupling for zero pin savings. The claim «драйвер ≪ 1.5К: инъекция ~2.2 мА через 1.5К сдвигает уровень на десятки мВ» is inconsistent with F10's 4 mA pad: at the spec point (UOL ≤ 0.4 V @ 4 mA) the driver is ~100 Ω, so toggling DPU injects ≈3.3 V·100/1600 ≈ 200 mV onto a driven D− — an order above «десятки мВ» and a real eye-margin perturbation precisely during the windows being measured. Architecturally it buys nothing: Р5 already reserves B2/B3/B4 and both NIIET boards leave B0-B4/C0-C4 free, yet the price is a manual re-park invariant («перед любым отпусканием шины — принудительный re-park») whose violation = bus disconnect, enforced in every ISR exit path forever.

**Доказательство:** PLAN.md Р10 (lines 154-159) vs PLAN.md F10 (line 39) «IO 3.3В … 4 мА»; research_gpio.md §5: «UOL ≤ 0.4 V @ IOL = 4.0 mA» (табл. 2.4, manual.txt:951-967) → Rdrv ≈ 100 Ω, not «≪ 1.5К». Pin scarcity: PLAN.md Р5 «резерв B2/B3/B4»; research_gpio.md §4: «C0-C4 and B0-B4 unused by both BSPs».

**Фикс:** Make DBG0 default to a reserved plain pin (B2), keep the DPU-toggle trick as an opt-in experiment; drop the mandatory re-park rule from the common ISR tail (it then applies only when the experiment is enabled). One row change in §7 defaults.

### arch-7 [MAJOR]
**Претензия:** The bit-identity regression gate covers only the V003 build («бит-идентичность V003-сборки»), but the zone edits land inside #if regions shared with the second existing cycle-exact variant, CH32V00x (dispatch clusters S:460-542, TX speed nibble S:760-764, keepalive S:690-728, tiny-boot) — and bootloader_v006 is a shipping build of that variant. A z-zone edit that silently reshuffles the V00x branch has no gate at all, so the guarantee protects the easy half and leaves the equally fragile half unguarded.

**Доказательство:** PLAN.md:86 «Гарантия: бит-идентичность V003-сборки до/после (check-цель Makefile)» — no mention of V00x. Shared conditional regions: rv003usb.S:460-542 (#if !defined(CH32V00x) … #else lui/jr), S:760-764 (per-variant CFGLR nibble), S:690-728. stack_portability.md §1: «bootloader_v006 (v002/5/6) … three .ld differ only in RAM LENGTH» — live targets. branch_notes.md WHY-#2: «same source, different chip, different cycle counts» — V00x timing is as cycle-fragile as V003's.

**Фикс:** Extend the check-цель to bit-compare all existing TARGET_MCU variants (CH32V003 + one CH32V00x build, e.g. bootloader_v006) — same mechanism, one extra compile in CI.

### arch-8 [MINOR]
**Претензия:** Р9 «вендорим только K1921VG015.h» commits to redistributing a 12.4k-line vendor header inside an MIT-licensed repo with zero license facts in the fact base (no SDK LICENSE terms are recorded anywhere in chip_info.md or research/*), and it contradicts the fact base's own recorded lesson from the py32 port (vendor scaffolding as submodule, don't copy). Unstated legal assumption; also a maintenance fork of the vendor header (SVD v1.14 today) with no update path.

**Доказательство:** Grep of doc/wg015/** for license terms: only an unrelated Milandr spec hit (research_bm310.md:18); chip_info.md §9 cites the SDK repo but records no license. Repo root has LICENSE (MIT-style rv003usb). branch_notes.md:72-73 (lessons): «Reuse the vendor SDK/template as a submodule for startup + linker + HAL; don't copy». PLAN.md Р9: «вендорим только K1921VG015.h».

**Фикс:** Add a P0 checklist item: read the NIIET SDK license (gitflic repo) and record the redistribution verdict in calibration.md/README; if redistribution is disallowed or unclear, fall back to the branch_notes submodule pattern (or generate a minimal own header from the SVD, which also shrinks the vendored surface).

### arch-9 [MINOR]
**Претензия:** The plan is not self-contained: P2 («Как v1 + подготовка z8»), Р8, Р9, §6 and §8 («Как v1 + …») delegate operative content to superseded v1, which exists only in git history (commit 471ec72) — while the plan's own R10 mitigation for session loss is «всё в файлах». An executor resuming after a context reset (the exact R10 scenario, which already killed 3 of 5 red-team lenses per STATE.md) must archaeology-dive git to learn what P2's skeleton or the DoD actually contains.

**Доказательство:** PLAN.md:219 «P2. Скелет таргета. Как v1 + подготовка z8», PLAN.md:274 «Как v1 + G1-вердикт…»; R10 (line 250) «всё в файлах, коммит на шаг, STATE.md»; STATE.md:16 «линзы boot/arch/complete — УПАЛИ на лимите сессии»; git log: PLAN v1 only in 471ec72, overwritten by 1d66714.

**Фикс:** Inline the ~10 lines of v1 content still load-bearing (P2 skeleton contents, DoD list, Р2 fork rationale) into v2.2, or keep PLAN_v1.md alongside; forbid «как v1» references in the final v3.

## Линза: complete

**Вердикт:** Through the completeness lens PLAN v2.2 is in good shape at its core: all 11 chip_info CORRECTIONS, open risks 1-3/5-7, all seven errata takeaways' primary consequences, and checklist items 1-6/8-11 land on identifiable phases, zones, or risk rows; spot-checked carrying facts F2 (LAT table 7.1, prefetch, I-cache), F5 (PLIC claim/complete + gateway + threshold semantics) and F8 (RSTSYS A55A, RTC_REG survival, RTC_REG[14] erratum) quote the research files accurately, and the v2.2 additions (P10 markers, CLK-B, defaults table, z8) introduce no new fact-base conflicts. The gaps cluster at the edges the timing/flash lenses didn't look at: the WCH XW custom instructions (checklist item 7, one site inside the counted TX loop) are absent from the zone spec and would break the P2 build and the TX ledger; the shared-C reboot-to-bootloader site (rv003usb.c:179-184) falls between Р3's three seams and Р8's bootloader-side coverage while P5's reflash criterion leans on it; P4's named demo (demo_hidapi) is not chip-clean (WCH WS2812 DMA/SPI driver, port-D pins); and the single shared PLIC GPIO line's user-pin sharing case defeats z5's claim-value dispatch. None of these invalidates the phase structure — each is a bounded spec addition — but all four majors-plus-blocker are of the 'discovered mid-phase, forces unplanned rework' class the plan exists to preempt, so they should be folded into v3 before P2 starts.

### complete-1 [BLOCKER]
**Претензия:** stack_portability §8 checklist item 7 — replace the 5 WCH XW custom-instruction sites — maps to NO zone, phase, or risk anywhere in PLAN.md. §2's zone list (z1–z8) is the port's implementation spec and covers GPIO constants, RX samples, TX stores, turnaround, ISR, keepalive, padding, trampolines — but not XW. Executed as written, the P2 skeleton ('скелет зелёный') fails to assemble on WG015 (XW_C_* are WCH-only encodings from ch32fun.h), and worse: the S:890 site sits inside the cycle-counted TX byte loop, so the forced replacement (2-byte compressed op → 4-byte lbu) changes instruction-stream length/alignment in a timed slot with no z7 ledger re-derivation budgeted for it — the exact latent-TX-timing class that surfaces only in P4 soak.

**Доказательство:** stack_portability.md §8.7: 'Replace XW instructions (5 sites, §7) with standard lbu/lhu/sb; re-balance the affected TX loop timing (S:890)'; rv003usb/rv003usb.S:439,477,485,665,890 — all unconditional, e.g. S:888-890: '// CH32v003 has the XW extension. // this replaces: lb s0, 0(a0) / XW_C_LBU(s0, a0, 0);' inside load_next_byte of the counted TX loop. PLAN.md contains zero occurrences of 'XW'; z7 rebalance scope is tied only to z3: 'z7 перебалансирует изменившиеся последовательности инструкций' (Р2 z3 note).

**Фикс:** Add the XW replacement as an explicit zone (or fold into z2/z3): define XW_C_LBU/LHU/SB → standard lbu/lhu/sb in the WG015 shim, and list S:890 (plus S:439/477/485/665) in z7's ledger-rebalance inventory so the TX byte-boundary slot is re-padded and verified by the P4 LA measurement.

### complete-2 [MAJOR]
**Претензия:** The APP-side reboot-to-bootloader path (rv003usb.c:179-184, inside the shared C protocol layer under RV003USB_USE_REBOOT_FEATURE_REPORT) is unmapped: Р3 declares 'C-слой единый. Три шва' (headers/clock, GPIO/EXTI init, IRQ macro — the branch_notes seam set) and §6 anti-scope forbids 'правки C-слоя сверх швов Р3', yet this fourth chip-dependent site uses five WCH registers that don't exist on WG015. Р8 covers only the bootloader-side entry (RTC_REG[0]+RSTSYS). P5's own done-criterion '100 циклов reflash без JTAG' is degraded without it — host-initiated re-entry into the bootloader from the running app is exactly this code.

**Доказательство:** rv003usb/rv003usb.c:179-184: 'FLASH->BOOT_MODEKEYR = FLASH_KEY1; ... FLASH->STATR = 1<<14; ... RCC->RSTSCKR |= 0x1000000; PFIC->SCTLR = 1<<31;' — flagged by stack_portability.md §3: 'Reboot-to-bootloader: FLASH->BOOT_MODEKEYR ... (c:179-184)'. PLAN.md Р3 lists only 'Три шва + usb_port_wg015.h' plus the descriptor-linker note; Р8 mentions RTC_REG[0]+RSTSYS solely as bootloader entry; no phase ports c:179-184. WG015 equivalent exists in fact base: research_usb_power.md §3 'RCU.RSTSYS (+C0h): write KEY[31:16] = A55Ah with RSTEN=1' + RTC_REG survives soft reset.

**Фикс:** Add a fourth declared seam: a REBOOT_TO_BOOTLOADER() macro in usb_port_wg015.h (WG015: sw flag→RTC_REG[0]; RCU->RSTSYS=A55A|RSTEN), wrap c:179-184 in it, and note the anti-scope exception explicitly; assign it to P5.

### complete-3 [MAJOR]
**Претензия:** P4 names demo_hidapi as the enumeration vehicle but its dependencies are not chip-clean and the plan has no cleaning step: demo_hidapi.c unconditionally includes and instantiates the WCH DMA+SPI WS2812 driver from ch32fun (WS2812DMA_IMPLEMENTATION + WS2812BDMAStart call), includes ch32fun.h, and its usb_config.h pins live on port D (WG015 has only A/B/C). P4 as written does not build; the plan must decide fork/#if/strip for the demo .c, which contradicts nothing explicitly but is unbudgeted work at the phase whose exit gate is 'энумерация, demo_hidapi'.

**Доказательство:** demo_hidapi/demo_hidapi.c:1 '#include "ch32fun.h"', :8-10 '#define WS2812DMA_IMPLEMENTATION ... #include "ws2812b_dma_spi_led_driver.h"', :53 'WS2812BDMAStart( 254/4 );'; demo_hidapi/usb_config.h:7-10 'USB_PORT D ... USB_PIN_DP 3 ... USB_PIN_DM 4 ... USB_PIN_DPU 5'; research_gpio.md §1: WG015 has '3 identical 16-bit ports A/B/C'. PLAN.md P4: 'энумерация, demo_hidapi; ≥100 переподключений' with no demo-porting step; branch_notes.md lesson 3 the plan builds on: 'Separate makefile per target family invoked over the same demo dirs; don't fork demos.'

**Фикс:** Add to P4 (or P2 skeleton scope): a wg015 demo variant decision — guard the WS2812/ch32fun-specific block behind #if !WG015 (LED payload becomes a no-op buffer) and supply per-target pin config; one sentence in the plan fixes the scope.

### complete-4 [MAJOR]
**Претензия:** chip_info 'Open risks' item 4 (all GPIO ports share ONE PLIC line 5) is only half-mapped. z5's foreign-IRQ hook — 'слабый символ, диспетчеризация по значению claim из MICC' — structurally cannot serve the most common sharing case: any user GPIO-pin interrupt arrives with the SAME claim value 5 as D−, lands inside the cycle-critical USB ISR, and the V003 parity mechanism (RV003_ADD_EXTI_MASK/RV003_ADD_EXTI_HANDLER, used by demo_exti) has no WG015 equivalent anywhere in the plan. A user pin IRQ enabled without support either gets its INTSTATUS bit never cleared (IRQ storm through the USB handler) or is silently dropped.

**Доказательство:** chip_info.md Open risks 4: 'Single GPIO IRQ line for all ports (PLIC #5) — fine if only D+/D- IRQ used, but any other GPIO IRQ shares it'; research_core_irq.md §4: 'GPIO: ONE shared vector 5 for all ports' and claim 'returns source number' — i.e. 5 for every GPIO pin; stack_portability.md §4: 'optional RV003_ADD_EXTI_MASK/RV003_ADD_EXTI_HANDLER asm hook to share the EXTI ISR with user pins (demo_exti/funconfig.h:6-7; S:78-83, 554-563)'. PLAN.md z5 offers only claim-value dispatch; Р7 priority scheme ('прочие нужные — ниже нашего') likewise addresses only distinct PLIC sources.

**Фикс:** Amend z5: same-source sharing is dispatched by GPIO INTSTATUS bits, not claim value — port the RV003_ADD_EXTI_MASK pattern (check INTSTATUS & ~USB_DMASK early, branch to weak user hook, W1C those bits) or explicitly anti-scope user GPIO interrupts on WG015 with a stated rationale.

### complete-5 [MINOR]
**Претензия:** The feature-flag surface (stack_portability §4) is mapped nowhere: the plan never says which of the ~13 usb_config.h flags are supported on WG015 for the P3/P4/P5 builds. Concretely: RV003USB_USB_TERMINAL pulls in lib/swio_self.h (WCH debug-module self-access via DMDATA0) and must be compile-excluded on WG015; and rv003usb.h:17-19 hard-errors unless FUNCONF_SYSTICK_USE_HCLK=1 — the shim must satisfy or zone this check. Both are one-line decisions, but undocumented they surface as confusing compile breaks for anyone touching demo_terminal or writing funconfig.

**Доказательство:** rv003usb/rv003usb.c:12-14: '#if RV003USB_USB_TERMINAL ... #include "../lib/swio_self.h" // Needed for unlocking DM'; stack_portability.md §1: 'lib/swio_self.h — WCH debug-module self-access, only for RV003USB_USB_TERMINAL'; §2: 'FUNCONF_SYSTICK_USE_HCLK 1 is mandatory (compile error otherwise, h:17-19)'. PLAN.md §7 defaults table covers HSE/pins/DBG/loader/CLI/marking/LA — no feature-flag row; §6 anti-scope silent on USB_TERMINAL.

**Фикс:** Add one row to §7: supported flags on WG015 = the demo_hidapi/bootloader set (OPTIMIZE_FLASH, HANDLE_IN_REQUEST, OTHER_CONTROL, HANDLE_USER_DATA, HID_FEATURES, BOOTLOADER, USE_REBOOT_FEATURE_REPORT); USB_TERMINAL = #error on WG015; shim defines FUNCONF_SYSTICK_USE_HCLK 1 (rdcycle is HCLK-synchronous).

### complete-6 [MINOR]
**Претензия:** R12's coverage is one-directional: trigger is 'TX-фронты вне 75-300 нс' (both bounds) but the only fallback listed — 'последовательные R/RC на D±' — fixes too-fast edges and makes too-slow edges worse. The fact base's only silicon-specific edge-rate signal points the other way: official erratum №3 documents host-side EOP misreads from weak/slow pad edges on this die, and the GPIO spec is only 4 mA drive with CL≤40 pF (an LS cable presents far more). If P4 measures edges slower than 300 ns, the plan has no named mitigation and the gate stalls.

**Доказательство:** PLAN.md R12: 'TX-фронты вне 75-300 нс (slew-контроля нет) | LA/скоп P4 | последовательные R/RC на D±'; research_errata.md TAKEAWAY 2: 'USB pads have NO internal D+ pull-up and with standard 1.5k the host misreads EOP; NIIET says use 510-750 Ohm. Suspect weak pad drive/slow edges — relevant to bitbang line interface too'; research_gpio.md §5: 'IOL/IOH ≤ 4 mA continuous', 'Load capacitance CL ≤ 40 pF'.

**Фикс:** Add the slow-edge branch to R12's fallback: minimize stub/bus capacitance (short leash, no series R), and as escalation an external buffer/driver (74LVC1G-class) on D± footprints planned at P0 alongside the series-R sites.

### complete-7 [MINOR]
**Претензия:** F7 states 'старт 0x8000_0000' as a load-bearing fact ('бутлоадер владеет вектором сброса', P5 layout hangs off it), but the fact base explicitly marks reset PC as an SDK-inferred GAP, unconfirmed by РП — and P0's exit checklist has no 'verify reset PC / boot-from-flash-base' item even though the check is free (any P0 blink proves it).

**Доказательство:** research_flash.md §5: 'Boot address: РП has no explicit reset-PC statement ... => CPU starts from flash base 0x8000_0000 (SDK-implied; РП-unconfirmed — GAPS)' and GAPS 7; research_usb_power.md GAPS 2: 'Reset PC value and mtvec reset value are not stated in РП'. PLAN.md F7 row carries no unverified-marker, unlike F2/F11 which carry hedges.

**Фикс:** Mark F7 as SDK-inferred and add one P0 exit-checklist word: 'blink linked at 0x8000_0000 boots from cold power-on (verifies reset PC)'.

### complete-8 [MINOR]
**Претензия:** Host-side operational completeness gaps in P5: (1) the plan silently reuses VID/PID 1209:B003 (bootloader) / 1209:D003 (demo) — the source itself carries an unresolved TODO to register the PID, and shipping a WG015 bootloader answering as the V003 bootloader's PID makes the new host CLI ambiguous about which protocol (blob ISA/flash geometry) it is talking to; (2) no udev/hidraw-permissions step for the hidapi-based CLI on Linux. Both are cheap now, annoying at P5.

**Доказательство:** bootloader/usb_config.h:41-42: '0x09, 0x12, //ID Vendor //TODO: register this in http://pid.codes/howto/ or somewhere. / 0x03, 0xb0, //ID Product'; demo_hidapi/usb_config.h:36-37 (1209:D003); PLAN.md §7 fixes only 'Хост-CLI | hidapi, busbflash-паттерн' — no VID/PID or host-setup row; Р8/P5 silent. The blob protocol is chip-specific per stack_portability.md §6: 'chip-specific flash programming lives in those host-supplied blobs → must be rewritten host-side for a new chip'.

**Фикс:** Add to §7 defaults: bcdDevice or PID distinguishes WG015 bootloader from V003 (e.g. bump ID Rev, keep 1209:B003 pending a pid.codes entry), and wg015hostcli README gets the udev rule one-liner; CLI must verify device identity before sending RAM blobs (wrong-chip blob = brick vector).

