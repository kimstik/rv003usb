# Code review — ветка wg015-port vs master (2026-09-02)

Инструмент: /code-review high (один проход, без независимой верификации).
Верификация по исходникам — оркестратор. Статус: ✔ подтверждено / ✘ отклонено / ~ частично.

| # | Файл | Находка | Вердикт | Действие |
|---|------|---------|---------|----------|
| 1 | wg015hostcli/wg015bflash.c:162 | readback парсится с buf+1, «должно быть buf+0» | ✘ ЛОЖНАЯ: hidapi возвращает report ID в buf[0], payload с buf[1]; репо-референс busbflash.c:49 сравнивает `buffer1+1` — CLI прав | нет |
| 2 ✅ | bootloader_dfu/dfu_v003.h:39 | SysTick->CNT читается, но никто не включает SysTick (TINY_BOOT ставит только PLL; HID-лодер делает `SysTick->CTLR=5` в boot.c:121) → счётчик 0 → цикл ожидания в dfu.c висит навсегда, кипэлайв-серво мёртв | ✔ РЕАЛЬНЫЙ БАГ | включить SysTick в dfu_port_flash_timebase_init() V003 |
| 3 ✅ | rv003usb.S:809 | В варианте WG015_TIMECRIT_TCM после `.section .text` (658) TX-движок не возвращается в .timecrit (переключение 809 только под CH32V00x) → TCM-фолбэк оставляет весь TX во флеше | ✔ РЕАЛЬНЫЙ БАГ фолбэка R1 | переключение секции для WG015 → .timecrit |
| 4 ✅ | dfu_015.h:20 | bwPollTimeout 50/8 мс «короче TERSR=100мс» | ~ аргумент ошибочен (TERSR — таймбейс «тактов на 100 мс», не длительность стирания), но время стирания неизвестно (GAP 6), 50 мс — догадка; HID-CLI ждёт 250 | поднять ERASE до 200 мс, пометить измерение |
| 5 ✅ | dfu.c:167 | DFU_UPLOAD отдаёт TX-петле указатель во флеш — нарушает правило Р3 (данные тактованного пути только в RAM; D-code латентность переменная по эррате №5) | ✔ | стейджить блок через RAM-буфер |
| 6 | blobs/blob_program_chunk.S:58 | юнит 64 пишет 16 слов с +0x04 по +0x40 → затирает TACCR..TPGSR (0x1C..0x34) ДО CMD=WR → импульс программирования с мусорным таймингом | ✔ (комментарий блоба сам это признаёт; SDK делает так же — вероятно баг SDK) | юнит 64 отклонять (badparam) до безопасного R6-зонда; CLI -u 64 → отказ |
| 7 ✅ | rv003usb.S:600 | RV003_ADD_EXTI_MASK на WG015 смотрит только INTSTATUS USB-порта; PLIC line 5 общая для A/B/C → пин на другом порту = livelock | ✔ ограничение | документировать: пользовательские GPIO-IRQ только на порту USB; TODO |
| 8 ✅ | rv003usb.S:11 | дубли смещений GPIO/PLIC_MICC при том, что K1921VG015_min.h их уже даёт (и C-макрос PLIC_MICC конфликтует по имени) | ✔ reuse | убрать локальные define, использовать имена хедера (PLIC_MICC_ADDR) |
| 9 ✅ | dfu_015.h:77 / bootloader_wg015.c:152 / blob_rescale | таймбейс-инициализация ×3 копии | ✔ reuse | inline WG015_FlashTimebase48MHz() в шиме; блоб (PIC, хост-сторона) остаётся |

Отброшено ревью после проверки (совпадает с моей оценкой): c.sw/c.lw с MASKLB-смещением (только не-WG015 ветки), PMURTC-гейтинг, контракт dfu_rx_ready, нумерация блоков DFU с 0, CRC/suffix в mkdfu.

## Статус исправлений (2026-09-03)

✅ Исправлено и проверено сборкой: 2 (SysTick на V003), 3 (TX-движок в .timecrit —
подтверждено: usb_send_data@0x40000266 в TCM-варианте), 4 (erase poll 200 мс),
5 (UPLOAD через RAM-буфер), 7 (ограничение общей PLIC-линии задокументировано в коде),
8 (дубли смещений убраны), 9 (общий WG015_FlashTimebase48MHz).
Остаётся 6: юнит записи 64 Б в blob_program_chunk затирает TACCR..TPGSR — отклонять
`-u 64` до проверки на железе (R6).
Размеры после правок: WG015 DFU 2876/3000, HID-лодер 2356/2600, V003 DFU 2340 (лимит 1920, WIP).
