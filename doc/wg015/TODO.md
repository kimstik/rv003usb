# WG015 TODO — точки пост-ревью и отложенные работы

Формат: пункт → триггер/фаза → источник. STATE.md — что сделано; здесь — что впереди.

## Пост-ревью (решения, назначенные на будущую точку)

1. **Трим-проход лодера, цель ≤2К** (сейчас 2352, гейт 2600 в Makefile).
   Триггер: P6, только после стабильности на кремнии. Кандидаты: startup/PLL −50..80 Б,
   main/timeout, exit-хвост. Источник: PLAN P6; обсуждение размера (V003=1920 некорректный
   бенчмарк — hand-stripped; v006 shared-C = 3324).
2. **Форк-гейт Р2**: макро-контракты в одном .S против форк-копии.
   Триггер: конец P4, метрика = новые WG015-условные строки (порог ~15%) или
   несводимость семантики. Источник: PLAN Р2, arch-2.
3. **tier-b ACK-first (3735518)**: переносить ли протокольную идею branchless-ветки.
   Триггер: P4/P6, только если измеренный турнэраунд > 7.5 бит-тайм. Источник: R9,
   branch_notes.md.

## Ревью артефактов агентов (капитанская приёмка не проводилась построчно)

4. **DFU-лодер** (bootloader_wg015_dfu, агент в работе): приёмка интеграционных точек
   (OTHER_CONTROL-диспатч, setup_request=2 буфер, ramfunc-размещение, адрес-гвард),
   size-гейт добавить как у HID-лодера. Триггер: по завершении агента.
5. **Построчное ревью скелета и бенчей** (wg015/, wg015_bench/, bootloader_wg015/blobs):
   агентские файлы машинно верифицированы сборкой, но выборочная сверка регистровых
   адресов с РП не делалась. Триггер: до первой прошивки (P0).
6. **ledger_static.md → внесение паддингов в .S**: сейчас леджер — документ; WGDELAY-
   макросы и мини-стаб для TX store-скью (2−B в pre_and_tok) не внесены в код.
   Триггер: сразу после бенчей 1-2 (известны B,G); скью-стаб можно внести и раньше.

## Хвосты кода (мелкие, помечены TODO(port) в файлах)

7. UART-retarget printf (сейчас stdout-заглушка) — P0, у бенчей UART уже есть.
8. check-identity цель Makefile.wg015 — сейчас stub; механика доказана вручную
   (scratchpad bitcheck) — оформить в цель с локальным тулчейном.
9. RV003USB_DEBUG_TIMING на WG015 → #error (TODO в шиме).
10. P10 TUNE-сборка: runtime-маски маркеров (x4-механика с масками из TCM) и
    jr-в-nop-след паддинг — каркас есть, полная реализация к P3-свипу.
11. Лицензия SDK НИИЭТ — вердикт в P0 (сейчас вендорим только самописный хедер,
    вопрос остаётся для будущих заимствований).

## Железо (P0-P6, детально в PLAN §4)

12. P0-чеклист: холодный blink@0x8000_0000, REFSEL-эксперимент, маркировка чипа,
    LA ≥100 МС/с, посадочные места R/буфера на D±.
13. Бенчи → B,G, флеш-профиль, MICC, integrity LAT=1 → паддинги → **G1-вердикт**.
14. HW-неизвестные: юнит записи 16/64 Б (R6), времена erase/program, LAT-ширина 3/4 бит,
    PMURTC-гейтинг, TCM-контеншн, mtvec MODE-writability, Zb*-наличие.
15. P3/P4: RX/энумерация, TUNE-свип, TX-фронты (R12, обе ветки), soak.
16. P5: 100 циклов reflash; SERVEN-анбрик задокументировать на живом чипе.

## Треки

17. **CLK-B (без кварца — целевой)**: out-of-spec PLL от HSI (REFSEL=0), VCO ниже
    минимума, HSI_TRIM-ручки, запрос NIIET/CloudBEAR. Параллельно, не блокирует.
    Серво-хук в z6 уже расслоен под это. Источник: PLAN Р4/CLK-B.
18. **BOOT-B (DFU)**: после приёмки (п.4) — по желанию выделить в отдельный бранч
    (cherry-pick); решение «какой лодер дефолтный» — по железу (размер/живучесть).
19. **Ре-мердж в апстрим** (cnlohr/rv003usb): после P6 — оформление PR с бит-идентичностью
    V003/V00x как главным аргументом безопасности; вопрос сабмодуля/вендоринга хедера.
19b. **BOOT-C: бэкпорт DFU на V003.** Триггер: после интеропа DFU с dfu-util на ВГ015.
    Оценка посадки ~1900-2200 Б при лимите 1920 (лезвие): минус HID/скратчпад, плюс
    DFU-диспатч (~350 Б на EC+XW), плюс WCH-флеш-рутина в RAM (~100 Б); резать UPLOAD,
    CRC32, строки. Приз: dfu-util вместо кастомного протокола на всей платформе.
    DFU-слой чипонезависим — бэкпорт = usb_config + флеш-рутина + вход.

## Профилактика процесса

20. Лимиты сессий: всё в файлах, коммит на шаг (R10) — правило действует постоянно.
21. Смена ревизии кремния = перезапуск P1-бенчей (R13); маркировка фиксируется в
    calibration.md.
