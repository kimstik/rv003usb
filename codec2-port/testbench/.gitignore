__pycache__/
# pinned codec2 checkout + host build (regenerated by build_oracle.sh)
build/
# derived decoder sources, host binaries, per-condition raw files, NISQA clone
out/build/
# downloaded external originals (fetch_ext.sh) and generated audio
out/ext/
out/wavs/
out/corpus/*.raw
# committed on purpose: out/*.html (the deliverables),
# out/results/*.json and out/corpus/manifest.json (the numbers behind them)
